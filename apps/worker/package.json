{
  "name": "@charms/worker",
  "private": true,
  "version": "0.1.0",
  "scripts": {
    "dev": "ts-node-dev --respawn --transpile-only src/index.ts",
    "build": "tsc -p ."
  },
  "dependencies": {
    "bullmq": "^5.79.1",
    "dotenv": "^17.4.2",
    "fastify": "^4.24.0",
    "zod": "^3.23.2"
  },
  "devDependencies": {
    "ts-node-dev": "^2.0.0"
  }
}
